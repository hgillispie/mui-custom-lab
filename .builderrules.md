# .builderrules - Sage & Pine Design System Usage Rules for AI

## 🎯 AI Development Guidelines - Sage & Pine Component System

### 🚨 CRITICAL: Component Usage Rules

**ALWAYS use our transformed components for UI elements**
**ALWAYS use generic MUI for layout, spacing, and utilities**

## 📋 Component Decision Matrix

| Component Type | Source | Rationale |
|---------------|---------|-----------|
| **Interactive UI** | Our System | Button, Input, Switch, etc. - Styled with Sage & Pine |
| **Layout** | Generic MUI | Box, Stack, Grid - No visual styling needed |
| **Typography** | Generic MUI | Typography - Inherits our font system |
| **Icons** | MUI Icons | Icons - Universal and consistent |
| **Non-Visual** | Generic MUI | Divider, Container - Pure utility |

## 🎨 Required Import Patterns

### ✅ CORRECT - Use Our Transformed Components
```jsx
// Interactive UI elements - ALWAYS use our system
import Button from './client/components/ui/Button';
import Input from './client/components/ui/Input';
import Card from './client/components/ui/Card';
import Switch from './client/components/ui/Switch';
import Select from './client/components/ui/Select';
import Checkbox from './client/components/ui/Checkbox';
import Alert from './client/components/ui/Alert';
import Modal from './client/components/ui/Modal';
import Table from './client/components/ui/Table';
import Tabs from './client/components/ui/Tabs';

// Layout and utilities - Use generic MUI
import { Box, Stack, Grid, Typography, Container, Divider, Paper } from '@mui/material';

// Icons - Use MUI icons
import { Search, Add, Delete, Settings, Home, User } from '@mui/icons-material';
```

### ❌ INCORRECT - Don't Mix Systems
```jsx
// WRONG - Don't use MUI for components we've transformed
import { Button } from '@mui/material';  // NO - use our Button
import { TextField } from '@mui/material'; // NO - use our Input
import { Switch } from '@mui/material'; // NO - use our Switch
```

## 🏗️ Sage & Pine Component Architecture

### Forms (Interactive Components)
```jsx
// Enhanced with Sage & Pine styling
import Button from './client/components/ui/Button';        // Gradient, hover effects
import Input from './client/components/ui/Input';          // Custom focus animations
import TextField from './client/components/ui/TextField';  // Alias for Input
import Select from './client/components/ui/Select';        // Styled dropdown
import Checkbox from './client/components/ui/Checkbox';    // Custom animations
import Radio from './client/components/ui/Radio';          // Enhanced selection
import Switch from './client/components/ui/Switch';        // iOS-style toggle
import Slider from './client/components/ui/Slider';        // Gradient tracks
```

### Navigation (Visual Components)
```jsx
// Styled with Sage & Pine theme
import AppBar from './client/components/ui/AppBar';        // Glassmorphism variants
import Tabs from './client/components/ui/Tabs';           // Animated indicators
import Menu from './client/components/ui/Menu';           // Enhanced animations
import Drawer from './client/components/ui/Drawer';       // Slide animations
import Breadcrumbs from './client/components/ui/Breadcrumbs'; // Styled paths
import Pagination from './client/components/ui/Pagination';   // Custom controls
```

### Data Display (Visual Components)
```jsx
// Enhanced presentation components
import Card from './client/components/ui/Card';           // Hover effects, variants
import Table from './client/components/ui/Table';         // Minimal/styled variants
import List from './client/components/ui/List';           // Enhanced animations
import Avatar from './client/components/ui/Avatar';       // Hover effects
import Chip from './client/components/ui/Chip';           // Custom colors
import Badge from './client/components/ui/Badge';         // Notification styles
import Tooltip from './client/components/ui/Tooltip';     // Enhanced positioning
import Accordion from './client/components/ui/Accordion'; // Smooth animations
```

### Feedback (Visual Components)
```jsx
// Notification and modal components
import Alert from './client/components/ui/Alert';         // Success/warning/error
import Dialog from './client/components/ui/Dialog';       // Backdrop blur
import Modal from './client/components/ui/Modal';         // Size variants
import Snackbar from './client/components/ui/Snackbar';   // Slide notifications
import Progress from './client/components/ui/Progress';   // Gradient animations
```

### Layout & Utilities (Generic MUI)
```jsx
// Use MUI directly - no visual styling needed
import { 
  Box,           // Layout container
  Stack,         // Flexbox layout
  Grid,          // Grid system
  Typography,    // Text elements
  Container,     // Page container
  Divider,       // Visual separator
  Paper,         // Base surface
  Backdrop,      // Overlay utility
  Portal,        // Rendering utility
  Fade,          // Transition utility
  Grow,          // Transition utility
  Slide,         // Transition utility
  Zoom           // Transition utility
} from '@mui/material';

// Icons - Use MUI icon library
import {
  Search, Add, Delete, Edit, Settings, Home, User,
  ArrowBack, ArrowForward, Close, Check, Error,
  Warning, Info, Visibility, VisibilityOff
} from '@mui/icons-material';
```

## 📱 Production Templates Available

### Complete Page Templates
```jsx
// Dashboard with analytics, charts, tables
import Dashboard from './client/templates/Dashboard';

// Authentication with validation, social login
import LoginForm from './client/templates/LoginForm';

// Settings panel with tabs, preferences
import SettingsForm from './client/templates/SettingsForm';

// Data management with CRUD, filtering
import DataTable from './client/templates/DataTable';
```

## 🎨 Standard Composition Patterns

### Form Pattern (Common Use Case)
```jsx
// ✅ CORRECT - Mixed approach
import { Stack } from '@mui/material';
import Card from './client/components/ui/Card';
import Input from './client/components/ui/Input';
import Button from './client/components/ui/Button';
import Alert from './client/components/ui/Alert';

<Card variant="contained">
  <Stack spacing={3}>
    {success && (
      <Alert severity="success">Form submitted successfully!</Alert>
    )}
    <Input
      label="Email"
      type="email"
      error={!!errors.email}
      helperText={errors.email}
      required
    />
    <Button variant="gradient" type="submit" fullWidth>
      Submit Form
    </Button>
  </Stack>
</Card>
```

### Settings Panel Pattern
```jsx
// ✅ CORRECT - Our components for UI, MUI for layout
import { Stack, Typography } from '@mui/material';
import Card from './client/components/ui/Card';
import Switch from './client/components/ui/Switch';
import Button from './client/components/ui/Button';

<Card variant="contained">
  <Stack spacing={3}>
    <Typography variant="h6">Preferences</Typography>
    <Switch 
      label="Email notifications"
      checked={settings.notifications}
      onChange={(checked) => updateSetting('notifications', checked)}
    />
    <Switch 
      label="Dark mode"
      checked={settings.darkMode}
      onChange={(checked) => updateSetting('darkMode', checked)}
    />
    <Button variant="gradient">Save Changes</Button>
  </Stack>
</Card>
```

### Data Display Pattern
```jsx
// ✅ CORRECT - Table component with MUI layout
import { Stack } from '@mui/material';
import Card from './client/components/ui/Card';
import Table from './client/components/ui/Table';
import Button from './client/components/ui/Button';
import Chip from './client/components/ui/Chip';

<Card variant="contained">
  <Table
    variant="minimal"
    columns={[
      { id: 'name', label: 'Name' },
      { id: 'status', label: 'Status' },
      { id: 'actions', label: 'Actions' }
    ]}
    rows={data.map(item => ({
      id: item.id,
      name: item.name,
      status: <Chip label={item.status} color="success" size="small" />,
      actions: (
        <Stack direction="row" spacing={1}>
          <Button variant="outlined" size="small">Edit</Button>
          <Button variant="error" size="small">Delete</Button>
        </Stack>
      )
    }))}
  />
</Card>
```

## 🎯 Component Prop Patterns

### Universal Props (All Our Components)
```jsx
variant="contained" | "outlined" | "minimal" | "gradient" | "glass"
size="small" | "medium" | "large"
showDemo={boolean}  // Preview mode
```

### Enhanced Component Examples
```jsx
// Button with Sage & Pine styling
<Button 
  variant="gradient"      // Pine/Sage gradient
  size="medium"
  loading={isSubmitting}
  startIcon={<Save />}
  fullWidth
>
  Save Changes
</Button>

// Input with custom focus animations
<Input 
  label="Email Address"
  variant="outlined"
  error={!!errors.email}
  helperText={errors.email || "Enter your email"}
  required
  fullWidth
/>

// Switch with iOS-style animation
<Switch 
  label="Enable notifications"
  checked={isEnabled}
  onChange={(checked) => setIsEnabled(checked)}
  variant="contained"
  helperText="Receive email updates"
/>
```

## 🚀 Page Structure Template

### Standard Page Layout
```jsx
import { Box, Container, Typography, Stack } from '@mui/material';
import Card from './client/components/ui/Card';
import Button from './client/components/ui/Button';

function StandardPage() {
  return (
    <Container maxWidth="lg">
      <Box sx={{ py: 4 }}>
        <Stack spacing={4}>
          <Typography variant="h4" component="h1">
            Page Title
          </Typography>
          
          <Card variant="contained">
            <Stack spacing={3}>
              <Typography variant="h6">Section Title</Typography>
              <Typography variant="body1">
                Content description goes here.
              </Typography>
              <Button variant="gradient">
                Primary Action
              </Button>
            </Stack>
          </Card>
        </Stack>
      </Box>
    </Container>
  );
}
```

## 🎨 Sage & Pine Design Token Reference

### Colors (CSS Variables)
- `--color-primary-500` - Pine `#31473a` (Dark green primary)
- `--color-secondary-500` - Sage `#7c8363` (Medium green secondary)
- `--color-success-500` - Success states
- `--color-warning-500` - Warning states
- `--color-error-500` - Error states

### Typography (Font System)
- `--font-family-body` - Outfit (body text)
- `--font-family-heading` - Poppins (headings)

### Component Usage
Our components automatically use these tokens, MUI components inherit the font system.

## 💡 AI Best Practices

### Do's ✅
- Use our components for all interactive UI elements
- Use MUI directly for layout (Box, Stack, Grid)
- Use MUI Typography for text (inherits our fonts)
- Follow consistent prop naming patterns
- Implement loading, error, and success states
- Use templates for complete page implementations

### Don'ts ❌
- Don't import MUI components we've transformed
- Don't skip error handling in forms
- Don't ignore responsive design
- Don't hardcode colors instead of using our components
- Don't mix component systems unnecessarily

## 🔍 Quick Reference Lookup

### Our Component File Paths
```jsx
// All our transformed components
'./client/components/ui/Button'
'./client/components/ui/Input'         // Also exports as TextField
'./client/components/ui/Card'
'./client/components/ui/Switch'
'./client/components/ui/Select'
'./client/components/ui/Alert'
'./client/components/ui/Modal'
'./client/components/ui/Table'
// ... 27 total components

// Production templates
'./client/templates/Dashboard'
'./client/templates/LoginForm'
'./client/templates/SettingsForm'
'./client/templates/DataTable'
```

## 🚀 Success Criteria

AI should:
1. **Import correctly** - Our UI components, generic MUI for layout
2. **Compose properly** - Cards for grouping, Stack for layout
3. **Handle states** - Loading, error, success patterns
4. **Use templates** - For complete page implementations
5. **Follow patterns** - Consistent prop usage and component combinations

**🌿 Sage & Pine Theme**: Professional nature-inspired design with Pine (`#31473a`) primary and Sage (`#7c8363`) secondary colors, using Outfit + Poppins typography for a modern, accessible aesthetic optimized for business applications.